{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^11.0.0",
    "purescript": "^0.11.3",
    "purescript-psa": "^0.5.0"
  }
}

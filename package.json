{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "ide": "purs ide server"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.14.0",
    "purescript-psa": "^0.8.2"
  }
}

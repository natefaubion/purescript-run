{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "ide": "purs ide server"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "slamdata/node-purescript#0.12",
    "purescript-psa": "^0.6.0"
  }
}

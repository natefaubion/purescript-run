{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "ide": "purs ide server"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "github:cryogenian/node-purescript-bin#master",
    "purescript-psa": "^0.6.0"
  }
}
